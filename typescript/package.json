{
  "name": "kodykatas",
  "version": "1.0.0",
  "type": "module",
  "description": "package to use when doing katas",
  "scripts": {
    "test": "vitest run",
    "compile": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "ts-node": "^10.1.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.9"
  }
}
