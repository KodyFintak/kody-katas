import {GildedRose} from '../src';

// kody, ellis, rob. sam, garrick
describe('test', () => {
    it('should add 1 + 2', () => {
        const rose = new GildedRose();
        rose.updateQuality();
    });
});
