import {GildedRose} from '../src';
// kody, ellis, rob. sam
describe('test', () => {
    it('should add 1 + 2', () => {
        const rose = new GildedRose();
        rose.updateQuality();
    });
});
